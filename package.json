{
  "name": "fio-banka",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev . --port 8080",
    "stamp": "node scripts/stamp-status.js",
    "deploy": "npm run stamp && npx wrangler pages deploy . && git checkout docs/project-status.html"
  },
  "devDependencies": {
    "wrangler": "^4.93.1"
  }
}
